version: "3"

services:
  backend:
    container_name: infisical-backend
    restart: always
    image: infisical/infisical:latest-postgres
    pull_policy: always
    env_file: stack.env
    ports:
      - 15080:8080
